<view class="container">
  <!-- 搜索框start -->
  <view class="search-bar">
    <view class="search-box">
      <image src="../../images/icon/search.png" class="search-img" />
      <text class="sear-input">搜索您想要的商品</text>
    </view>
  </view>
  <!-- 搜索框end -->

  <!-- 侧边栏start -->
  <view class="main">
    <!-- 左边 -->
    <scroll-view scroll-y="true" class="leftmenu">
      <block wx:for="{{categoryList}}" wx:key="categoryId">
        <view class="menu-item {{selIndex==index?'active': ''}}" data-index="{{index}}" data-id="{{item.categoryId}}" bind:tap="onMenuTab">
          {{item.categoryName}}
        </view>
      </block>
    </scroll-view>
    <!-- 左边 -->

    <scroll-view scroll-y="true" class="rightcontent">

      <view class="adver-map">
        <image src="{{categoryImg}}" mode="widthFix" />
      </view>

      <view class="cont-item">
        <block wx:for="{{productList}}">
          <view class="show-item" bind:tap="toProdPage" data-prodid="{{item.prodId}}">
            <view class="more-prod-pic">
              <image src="{{item.pic}}" mode="widthFix" class="more-pic"/>
            </view>

            <view class="prod-text-right">
              <view class="prod-text more">{{item.prodName}}</view>
              <view class="cate-prod-info">{{item.brief}}</view>
              <view class="prod-price more">
                <text class="symbol">￥</text>
                <text class="big-num">{{wxs.parsePrice(item.price)[0]}}.</text>
                <text class="small-num">{{wxs.parsePrice(item.price)[1]}}</text>
              </view>
            </view>

          </view>
        </block>
      </view>
      <view>
      </view>
    </scroll-view>
  </view>
  <!-- 侧边栏end -->
</view>
<wxs module="wxs" src="../../wxs/number.wxs"></wxs>

